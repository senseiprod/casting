export const environment = {
    production: false,
    apiUrl: "http://localhost:8080", // Base URL for the API
    mockDelay: 500, // Delay in milliseconds for mock API responses
    useMockData: true,
  }
   