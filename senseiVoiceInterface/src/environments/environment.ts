export const environment = {
    production: false,
    apiUrl: "https://api.castingvoixoff.ma", // Base URL for the API
    mockDelay: 500, // Delay in milliseconds for mock API responses
    useMockData: true,
  }
   